# Agent config (editable)
CONFIG = {
  "auto_merge_policy": "PR-only",  # PR-only | limited | full
  "daily_hq_cap_usd": 5,
  "max_auto_pushes_per_day": 20,
  "safe_paths": ["docs/", "README.md", "scripts/"],
  "llm_model": "openai/gpt-5-mini",
  "hq_alias": "hq",
}
